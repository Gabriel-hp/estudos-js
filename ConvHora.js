//Crie um programa que converte um valor de tempo dado em horas para minutos. 

function ConvHora(){
    const Hora = 12;

    const convs = Hora * 60

    console.log ("suas horas são " + convs, "minutos")
}
ConvHora()